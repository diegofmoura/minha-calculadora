# Projeto Python com Script Executável

Este projeto contém um script simples em Python e um arquivo `.sh` para facilitar a execução via terminal.

## 📌 Estrutura do projeto

- `script.py` → Código principal em Python
- `executar.sh` → Script executável para rodar o código
- `README.md` → Documentação do projeto

## ▶️ Como executar o arquivo .sh

1. Abra o terminal na pasta do projeto
2. Dê permissão de execução (caso necessário):

```bash
chmod +x executar.sh
